# Sprint 3

## User Story:
Campus Community User can search and filter reports by type, status, date, and location

| Test Case | Input | Expected Output | Actual output |
|---|---|---|---|
| 1 (Success) | **Precondition:** User is not logged in. 1. Open the site 2. Check the main menu | The menu shows `Home`, `Reports`, `About`, `Contact`. Items `Create Report`, `My Reports`, and `All Reports` are hidden | Passed. Menu reorganization worked. Public navigation showed only the public items and hidden protected items stayed unavailable |
| 2 (Success) | **Precondition:** User is logged in as Campus Community User. 1. Open the site 2. Check the main menu | The menu shows `Home`, `Reports`, `My Reports`, `Create Report`, `About`, `Contact`. `All Reports` is hidden | Passed. Campus user menu displayed `My Reports` and `Create Report`, while `All Reports` was not shown |
| 3 (Success) | **Precondition:** User is logged in as Security Office Staff. 1. Open the site 2. Check the main menu | The menu shows `Home`, `Reports`, `My Reports`, `Create Report`, `All Reports`, `About`, `Contact` | Passed. Staff menu displayed all expected navigation items, including `All Reports` |
| 4 (Success) | **Precondition:** Reports menu links are available. 1. Click `Reports` 2. Click `My Reports` 3. Log in as staff and click `All Reports` | Each menu item opens `/reports/` with the correct `scope` parameter (`published`, `mine`, `all`) | Passed. Menu links worked correctly and routed to the proper scope-based results pages |
| 5 (Success) | **Precondition:** Reports with different visibility exist. 1. Open `/reports/` 2. Open `/reports/?scope=published` | Only published reports are shown | Passed. Published scope showed only published reports |
| 6 (Success) | **Precondition:** Logged-in campus user has own reports, including unpublished ones. 1. Open `/reports/?scope=mine` | Only the current user’s own reports are shown, including unpublished reports owned by that user | Passed. `My Reports` worked correctly and showed only owner records |
| 7 (Failure) | **Precondition:** Logged-in campus user is not staff. 1. Open `/reports/?scope=all` | The system must not show all reports. It must fall back to published-only results | Passed. Non-staff user could not access full report list through `scope=all`; only published reports were shown |
| 8 (Success) | **Precondition:** Logged-in staff user exists and multiple reports exist. 1. Open `/reports/?scope=all` | All reports are shown to staff, including unpublished reports | Passed. Staff user could access all reports successfully |
| 9 (Failure) | **Precondition:** Any user or guest. 1. Open `/reports/?scope=abc` | Invalid scope is ignored and the system falls back to published-only results | Passed. Invalid scope did not break the page and published-only results were shown |
| 10 (Success) | **Precondition:** Reports of both `Found` and `Lost` types exist. 1. Open Reports page 2. Select `Found` in the type filter 3. Click `Filter` 4. Repeat for `Lost` | Only reports of the selected type are shown | Passed. Type filter worked correctly for both found and lost reports |
| 11 (Success) | **Precondition:** Reports with statuses `Pending`, `Approved`, and `Rejected` exist. 1. Select a status filter 2. Click `Filter` | Only reports with the selected status are shown | Passed. Status filter worked correctly |
| 12 (Success) | **Precondition:** Reports with different locations exist. 1. Enter a location value such as `library` 2. Click `Filter` | Only reports whose location matches the entered text are shown | Passed. Location filter worked correctly |
| 13 (Success) | **Precondition:** Reports contain searchable text in title, description, or location. 1. Enter a keyword such as `wallet` in Search 2. Click `Filter` | Reports matching the keyword in title, description, or location are shown | Passed. Search filter worked correctly |
| 14 (Success) | **Precondition:** Reports exist on different creation dates. 1. Enter `date_from` 2. Enter `date_to` 3. Click `Filter` | Only reports within the selected date range are shown | Passed. Date range filter worked correctly |
| 15 (Success) | **Precondition:** Reports exist that can match multiple conditions. 1. Open `My Reports` or `All Reports` 2. Set multiple filters such as type, status, and location 3. Click `Filter` | The system returns only reports matching all selected criteria and keeps the same scope | Passed. Combined filtering worked correctly and scope was preserved |
| 16 (Success) | **Precondition:** Filters are applied. 1. Apply any filters 2. Observe the form values | Filter fields remain filled with the selected values after submit | Passed. Current filter values were preserved in the UI after filtering |
| 17 (Success) | **Precondition:** User is inside `Reports`, `My Reports`, or `All Reports` with filters applied. 1. Click `Reset` | Filters are cleared and the page returns to the same scope without extra filters | Passed. Reset button worked correctly and preserved the selected scope |
| 18 (Success) | **Precondition:** Reports page is opened in each scope. 1. Open `Reports` 2. Open `My Reports` 3. Open `All Reports` as staff | The page shows a clear label: `Published Reports`, `My Reports`, or `All Reports` | Passed. Scope label was shown correctly for each results view |
| 19 (Success) | **Precondition:** At least one filter returns matching rows. 1. Apply filters that produce results | The page shows a results count and the table includes a `Status` column | Passed. Results count and status column were displayed correctly |
| 20 (Success) | **Precondition:** Apply filters that return no matching rows. 1. Enter filters with no expected matches 2. Click `Filter` | The system shows a clear empty-state message instead of a broken or empty table | Passed. Empty-state message displayed correctly: no matching reports were found |
| 21 (Failure  Fixed) | **Precondition:** Search logic is implemented with multiple searchable fields. 1. Use search together with other filters 2. Verify results consistency | Search should combine correctly with other filters and not produce incorrect queryset behavior | Initially risk identified in search implementation using queryset unions. Fixed by replacing it with `Q(...)` conditions. After fix, search worked correctly with combined filters |
| 22 (Success) | **Precondition:** Filtered results are displayed in the table. 1. Click `View` for any filtered report | The system opens the correct report detail page | Passed. `View` link continued to work correctly from filtered results |
